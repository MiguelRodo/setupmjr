[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "setupmjr"
version = "0.1.0"
description = "Thin Python wrapper for the setupmjr CLI"
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["setupmjr"]
